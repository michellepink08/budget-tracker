import { Card } from "@/components/ui/card";
import { formatMoney } from "@/lib/money";
export function MonthlyPlanSummary({ currency, values }: { currency: string; values: { expectedIncome: number; actualIncome: number; incomeDifference: number; plannedSpending: number; actualSpending: number; spendingDifference: number; unallocated: number } }) { return <div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><Card className="p-4"><p className="text-sm text-muted-foreground">Expected / received income</p><p className="font-semibold">{formatMoney(values.expectedIncome,currency)} / {formatMoney(values.actualIncome,currency)}</p><p className="text-sm">Difference {formatMoney(values.incomeDifference,currency)}</p></Card><Card className="p-4"><p className="text-sm text-muted-foreground">Planned / actual spending</p><p className="font-semibold">{formatMoney(values.plannedSpending,currency)} / {formatMoney(values.actualSpending,currency)}</p><p className="text-sm">Difference {formatMoney(values.spendingDifference,currency)}</p></Card><Card className="p-4"><p className="text-sm text-muted-foreground">Unallocated expected income</p><p className="font-semibold">{formatMoney(values.unallocated,currency)}</p></Card><Card className="p-4"><p className="text-sm text-muted-foreground">Cycle</p><p className="font-semibold">Plan vs actual</p></Card></div>; }
